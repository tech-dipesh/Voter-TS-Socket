import React from 'react'
import { Socket } from 'socket.io-client'

export const Api = () => {
  const webSocket=new {origin: "localhost:3000",
    Credential: true
  }
  return (
    <>
    
    </>
  )
}
